```
go to github.com to create new repo by clicking '+' sign
$cd <folder name>
$git init
$git add .
$git commit -m "message"
$git remote add origin https://github.com/<user>.git
$git push origin master

```
